---
interface Props {
    imgurId: string;
}

const { imgurId } = Astro.props;
---

<video autoplay loop muted playsinline>
    <source src={`https://i.imgur.com/${imgurId}.mp4`} type="video/mp4" />
    <p>Your browser doesn't support this embedded video.</p>
</video>
